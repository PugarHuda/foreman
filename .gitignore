.env
.env.local
.env.*.local
node_modules/
artifacts/
cache/
types/
.next/
out/
*.log
*.tsbuildinfo

# test + tooling output
test-results/
playwright-report/
.playwright-mcp/
blob-report/

.vercel

# Playwright's raw recordings before record-demo renames them
docs/page@*.webm

# Ingested telemetry — a plant's own readings, not the repo's
data/

# Assembled by scripts/submission.mjs from files that already live elsewhere.
submission/

# Copied in at render time from public/demo.mp4 — one canonical copy, not two.
video/public/demo-source.mp4
